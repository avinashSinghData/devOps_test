name: CI Pipeline

on:
  push:
    branches:
      - first_branch

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Validate repo structure
        run: |
          echo "Checking repository..."
          ls -R
